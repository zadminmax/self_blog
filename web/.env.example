API_URL=http://127.0.0.1:8080
# 须带协议；勿只写 localhost:3000，否则 metadataBase 会抛 Invalid URL 导致整站白屏
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# 若需在浏览器端直连 API（一般不需要）：
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8080
