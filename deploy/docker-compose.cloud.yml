# 与根目录 docker-compose.yml 一起使用：
#   docker compose -f docker-compose.yml -f deploy/docker-compose.cloud.yml --env-file .env up -d --build
# 作用：不将 Postgres 暴露到公网；可与 deploy/.env.production 配合。

services:
  postgres:
    ports: []

  api:
    environment:
      APP_ENV: production
