# 本机执行 ./deploy/build.sh 前复制为 deploy/.env.build 并填写（用于前端构建期注入）
# 须与线上一致（https + 你的子域）

VITE_API_URL=https://api.example.com
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_SITE_URL=https://www.example.com
