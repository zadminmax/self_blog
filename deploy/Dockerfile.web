# 与 build.sh 里 Linux 构建环境一致（glibc）；Next standalone 仍是 Node 进程，需带上 traced 的 node_modules
FROM node:20-bookworm-slim
WORKDIR /app
ENV NODE_ENV=production
ENV PORT=3000
ENV HOSTNAME=0.0.0.0
COPY deploy/artifacts/web /app
EXPOSE 3000
CMD ["node", "server.js"]
